{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <div>
    <form action="/" method="POST">
        <label for="nom"> Nom de l'entreprise : </label><input type="text" name="nom" id="nom" placeholder="Entrez le nom de l'entreprise" /><br/><br/>
        <label for="montant"> Montant : </label><input type="text" name="montant" id="montant" placeholder="Entrez votre montant" /><br/><br/>
        <input type="submit" name="btOK" value="Valider">
    </form>

        <div class="flex flex-col w-screen h-screen justify-center items-center">
            <h3 id="city" class="text-8xl font-extrabold mb-8">Cours de {{ displayName }}</h3>
            <h4 id="temp" class="text-5xl font-bold mb-3">{{ regularMarketPrice }}</h4>
        </div>
    </div>
{% endblock %}
